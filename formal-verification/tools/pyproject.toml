[project]
name = "spec-verify"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = ["click>=8.0", "openai>=1.0", "python-dotenv>=1.0"]

[project.scripts]
spec-verify = "spec_verify.cli:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
